# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Data & checkpoints (never commit these)
/data/
/checkpoints/
*.bin
*.idx
*.safetensors
*.pt

# Logs
wandb/
logs/
*.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Keep the reports directory but not heavyweight artifacts
reports/**/*.png
!reports/**/figures/*.png

# 本机专用的 uv 缓存配置（绝对路径，不入库）
uv.toml

# 编译缓存等（Rangpur 上放在项目目录内，便于收尾时一并删除）
.cache/
