# 算力、时间与成本

---

## 一、先把账算清楚

**训练总 FLOPs 的经验公式**：

```
C ≈ 6 · N · D
     N = 非嵌入参数量
     D = 训练 token 数
     6 = 前向 2 + 反向 4（每参数每 token 的乘加次数 × 2）
```

主线 500M：`N = 4.52e8`（非嵌入），`D = 5.1e10` →

```
C = 6 × 4.52e8 × 5.1e10 = 1.38e20 FLOPs
```

可选 1B：`N = 9.92e8`，`D = 2.5e10` →

```
C = 6 × 9.92e8 × 2.5e10 = 1.49e20 FLOPs      # 与 500M 方案算力相当
```

**注意这两个方案总算力几乎一样，但花法不同**：500M 把算力花在"同一批参数上多看数据"（100 tok/param），1B 把算力花在"更多参数上少看数据"（25 tok/param）。Chinchilla 说后者对**训练**更优，但前者对**推理**更优，且实际更好用——这正是 SmolLM2、Qwen2.5 等现代小模型的选择。

**换算成时间**：

```
时间 = C / (GPU数 × 峰值FLOPS × MFU)
```

MFU（Model FLOPs Utilization）是实际达到的算力利用率。参考值：

| 情形 | MFU |
|---|---|
| 第一次跑，没调优 | 15–25% |
| 常规调优（compile + flash-attn + 合理 batch） | 35–45% |
| 深度优化（大厂水平） | 50–60% |

**下面所有估算按 MFU=40% 计算，第一次跑请乘以 1.5–2。**

---

## 二、时间表

峰值算力（bf16 稠密，非稀疏）：H100 SXM ≈ 989 TFLOPS，A100 ≈ 312 TFLOPS，RTX 4090 ≈ 165 TFLOPS，L40S ≈ 362 TFLOPS。

### 500M 模型（主线）

| 硬件 | 10B tokens（Chinchilla 最优） | 51B tokens（本项目目标） |
|---|---|---|
| 1× RTX 4090 24G | ~5–6 天 | 不现实 |
| 1× A100 80G | ~2.5 天 | ~13 天 |
| 1× H100 80G | ~19 小时 | ~4 天 |
| 8× A100 40G | ~6 小时 | ~30 小时 |
| ★ **8× H100 80G** | **~2.5 小时** | **~14 小时** |
| TPU v4-32（TRC 免费） | ~4 小时 | ~20 小时 |

### 1B 模型（可选升级）

| 硬件 | 22B tokens（Chinchilla 最优） | 25B tokens（推荐） | 50B tokens |
|---|---|---|---|
| 1× RTX 4090 24G | 不现实（且显存不够） | 不现实 | 不现实 |
| 1× H100 80G | ~3.8 天 | ~4.4 天 | ~9 天 |
| 8× A100 40G | ~36 小时 | ~41 小时 | ~3.5 天 |
| ★ **8× H100 80G** | **~11.5 小时** | **~13 小时** | **~26 小时** |

1B 的额外约束：单卡显存 26 GB 放不进 4090，必须上 ZeRO-2/FSDP 或 8-bit optimizer（见 [02-architecture.md](02-architecture.md) §1.4）。**这意味着 1B 不能在单卡上完整调试**，所有 bug 都得在多卡环境里查——这是 1B 真正的成本，比多花的那点钱大得多。

**建议**：先按 500M 走完 P0–P8 拿到完整报告，再用同一套代码跑 1B。这样 1B 那次训练是在一个已经验证过的流水线上跑，风险低得多，同时给缩放律曲线添上第 5 个点。

### 其它规模（做缩放律实验用）

| 模型 | tokens (20×) | 8×H100 时间 |
|---|---|---|
| 30M | 0.6B | ~6 分钟 |
| 60M | 1.2B | ~12 分钟 |
| 120M | 2.4B | ~25 分钟 |
| 250M | 5B | ~1.2 小时 |

四个点全跑一遍约 2 小时 = $40，非常划算。

---

## 三、平台对比

### 海外

| 平台 | H100 单价（/卡/时） | 优点 | 缺点 |
|---|---|---|---|
| ★ **Lambda Labs** | $2.5–3.3 | 环境干净、按需 8 卡整机、口碑好 | 经常缺货，要蹲 |
| ★ **RunPod** | $2–3（Community 更低） | 秒开、有竞价、社区资源便宜 | 社区节点质量参差，磁盘 IO 可能慢 |
| **Vast.ai** | $1.5–2.5 | 最便宜 | 个人矿卡，可靠性最差，只适合可中断任务 |
| **Prime Intellect** | 聚合比价 | 一站式找最便宜的 | 中间商 |
| **Hyperbolic** | $1.5–2.5 | 便宜 | 较新 |
| **Modal** | 按秒计费 | serverless，无运维，适合数据处理并行 | 单价高，长训不划算 |
| **CoreWeave / Together** | 议价 | 大规模稳定 | 面向企业，起订量大 |
| ★ **Google TPU Research Cloud** | **免费** | 通过申请可免费用 TPU v4 一个月（可续）| 需用 JAX 生态（MaxText / Levanter / EasyLM），PyTorch 迁移成本高 |

**8×H100 整机行情：约 $16–26/小时**，竞价实例便宜 50–70%。

### 国内

| 平台 | 说明 |
|---|---|
| ★ **AutoDL** | 学生党主力。4090 约 ¥2/时，A100 约 ¥6–8/时。适合 P0–P4 的单卡阶段 |
| **恒源云 / 揽睿星舟 / 智星云** | 同类，比价用 |
| **火山引擎 / 阿里云 PAI 灵骏 / 腾讯云 / 百度百舸** | 多机多卡走这些，注意新用户代金券和高校计划 |
| **百度 AI Studio / 魔搭 ModelScope** | 有免费算力额度，够跑通流程 |
| **Kaggle** | 每周免费 30h T4×2，只能跑通，不能预训练 |

---

## 四、成本明细（复述自路线图）

| 项目 | 明细 | 成本 |
|---|---|---|
| P0 参考项目 | 单卡 4090 ~10h | ¥20 |
| P2 数据处理 | 大内存 CPU 机 ~40h | ¥100 |
| P4 冒烟 | 单卡 H100 ~5h | $12 |
| P5 缩放+消融 | 8×H100 ~3h | $60 |
| P6 正式预训练 | 8×H100 ~15h | $300 |
| P7 退火+SFT+DPO | 8×H100 ~3h | $60 |
| P8 评测 | 单卡 ~8h | $20 |
| 对象存储 | ~2TB·月 | $40 |
| **合计** | | **≈ $500** |

**降本三招**：
1. **竞价实例** — 便宜 50–70%，代价是必须实现 2000 步一存 checkpoint + 自动重启恢复（这本来就该做）
2. **只训 10B tokens** — P6 从 $300 降到 $60，模型明显更弱但**学到的东西完全一样**。预算紧就选这个
3. **TPU TRC** — 免费，但要接受 JAX 学习成本

---

## 五、租机操作要点

1. **先租最小的确认环境**：1 卡跑 30 分钟，确认 CUDA 版本、`torch.compile` 能过、数据能读、W&B 能连
2. **数据别放在实例本地盘** — 放对象存储（S3/R2/OSS），实例启动时拉取或直接流式读
3. **checkpoint 立刻同步到对象存储** — 实例被抢占是常态，不是意外
4. **用 tmux / nohup** — SSH 断了训练不能停
5. **算好再开机** — 开机前用公式估一遍时间，跑起来 10 分钟内核对实际吞吐与估算是否一致，差太多立刻停机排查
6. **监控要能推到手机** — W&B alert 或简单的 webhook，半夜炸了要能知道

---

## 六、MFU 上不去的排查顺序

1. dataloader 打不满 GPU（看 `step_time` 里的等待占比）→ 加 worker、预取、把数据放本地 NVMe
2. 没开 `torch.compile` 或被 graph break → `TORCH_LOGS=graph_breaks` 查
3. 没用 FlashAttention → 确认走了 `F.scaled_dot_product_attention` 的 flash 后端
4. batch 太小 → 增大 micro-batch 直到显存打满，再用梯度累积凑全局 batch
5. 维度不是 128 的倍数 → tensor core 对齐（本项目 d=1280、ffn=3456 已对齐）
6. 通信瓶颈（多卡）→ 检查是否 NVLink，`NCCL_DEBUG=INFO` 看拓扑
7. 梯度检查点开太多 → 500M 单卡显存够，能关就关（省 30% 时间）
