# 训练日志

按天记录。这是技术报告第 5、10 节的原始素材，训练期间随手记，不要事后补。

格式见 [docs/08-report-template.md](../docs/08-report-template.md)。

## 2026-09-19 · P1 分词器
- P1-1：写测试时发现 HF BpeTrainer 在语料不够时会默默少给词表（要 600 给 427），train_bpe 加了大小检查
- P1-1：HF tokenizers 默认把文本里的 "<|endoftext|>" 字样识别成 id 0，设 encode_special_tokens=True 关掉
- P1-2：规划里的 BAAI/CCI3-HQ、The Stack 系列都要登录同意条款；python-edu 只有 blob id 没有代码内容。换成 chinese-fineweb-edu-v2、github-code-clean
- P1-2：github-code-clean 里 Go 的标签是 "GO"，第一次抽样漏掉了 Go，重抽
- P1-2：1 GB 语料训 32k 用时 5.7 分钟、约 2 GB 内存。中文 0.70 tokens/字（≈ Qwen2.5），英文 1.45 tokens/word（比大词表差 7–11%）
- P1-2：词表扫描 16k/32k/48k/64k 按 250M 模型的 FLOPs/字节算，32k 正好是谷底；数字一位一切多花 2.2%
- P1-2：zh_wiki 一半是繁体，所有分词器在它上面都更差；P2 决定是否转简体
- 路线图英文验收 1.35 做不到（64k 也只有 1.37），按实测改标准

## 2026-09-20 · P2-1 质量过滤、P2-2 去重
- P2-1：第一版 Gopher 规则在 fineweb-edu 上丢 3.8%（它已被同样规则过滤过，应接近 0）。对照 datatrove 发现分母和重复 n-gram 计数方式不同，改后 99.2%
- P2-1：中文按字套 Gopher 阈值，zh_web 只剩 40%；n×3 后 95.2%，被丢的是整段抄了几遍的文档
- P2-1：finemath 的论坛界面文字（"Reply"）让"重复行按条数"误杀 10%，数学只按字符判断
- P2-2：MinHash 128 个哈希在中文维基上精确率 51–84%（模板条目 J≈0.75，估计噪声推过阈值，赢家的诅咒）；256 个时 86–96%。暴力枚举 2000 篇作为标准答案
- P2-2：不复核 LSH 候选对，精确率掉到 7%；原单元测试没抓到，补了 test_bucket_collision_is_not_a_duplicate
- P2-2：段落去重在代码里会删 Apache 许可证头，代码不做
- 两次用 heredoc 里的 Python 字符串改源码，把正则里的 \n 写成了真换行，改用 Edit 工具

## 2026-09-20 · P2-3 去污染
- 下载 11 个评测集的验证/测试划分（8.1 万道题，25 MB）。CMMLU 仓库已改名 lmlmcat/cmmlu；第一次抽出 0 题，zip 内路径是 test/ 不是 /test/
- 13-gram 每个汉字算一个单位时，zh_web 9% 被判污染，几乎都是 C-Eval 引用的固定表述、名句（"全面建设社会主义现代化国家"正好 13 字）。汉字按半个词算后降到 15/2000
- 用 1 GB 分词器语料统计通用片段（《独立宣言》、美国法案开头、十六进制字符表、政治表述），≥3 次的 220 个排除
- 最终：zh_web 11/2000、en_web 1/2000（WikiHow 原文，HellaSwag 出处）、math 2/2000
- 局限：C-Eval 27%、CMMLU 38%、PIQA 测试集 66% 的题比窗口短，查不出来；把短题整道放进索引没用（窗口定长，永远不等）

## 2026-09-20 · P2-4 分词打包
- 两步落盘：每个子集 docs.bin + docs.idx（每篇前放 EOT）→ 划验证集、按配比混合、在文档边界切分片，写 meta.json
- 混合按 token 数做比例轮询：7:3 时任意连续 100 篇的比例偏差 ≤ 0.02
- 训练端：配置 tokenizer 后核对分片指纹和每个分片的大小（为 P6 从对象存储拷贝准备）；每个 val_<子集>.bin 单独报 loss
- 迷你数据（校准文档 1.4 万篇 → 1000 万 token）本机 ladder_s1 训 150 步 3.5 分钟：各子集 val loss 都在降，中文比英文高约 1
- 分词速度 9–20 MB/s（HF tokenizers，本机）

## 2026-09-20 · P2-5 完整流水线，2B tokens 落地
- 阶段划分：fetch（过滤+签名+查污染，可并行）/ clean（去重+段落去重，必须串行）/ tokenize / pack / report
- 规划两个坑：只探第一个行组且不套过滤规则（代码、维基都取少了）；最后一个任务被截断导致改目标量后分片编号错位、已下分片被错误复用。改成探 3 个分散行组 + 套过滤规则、每个任务固定 8 个行组
- 实跑：读 11.1 GB 文本、214 万篇；fetch 45 分钟（两次合计）、其余 23 分钟；磁盘峰值 15 GB
- 产出 2.000B tokens、20 个分片、3.8 GB；维基两个子集不够配额，重复 1.4 / 1.3 轮
- 发现：chinese-fineweb-edu-v2 的 00312.parquet 整段是英文，"汉字 ≥40%"挡下 8.9 万篇；近似重复 0.9%（校准时 2000 篇里一篇都没有）；跨子集重复仅 424 篇；zh_web 命中 C-Eval 1566、CMMLU 846
- 真实分片冒烟：ladder_s1 60 步，指纹核对通过，各子集 val loss 都在降
- 又一次踩 heredoc 转义坑（备忘里已记），改用 Edit 工具
